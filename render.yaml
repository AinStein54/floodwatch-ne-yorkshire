services:
  - type: web
    name: floodwatch-ne-yorkshire
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    healthCheckPath: /api/health